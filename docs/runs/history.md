# Runs, one line each

Newest last. The evidence itself is never kept as folders: only the latest report that still proves something stays on
disk (`Tests/Pickle/Evidence/`, ignored by git). A run that needs more than a line gets a file next to this one.

- 2026-09-21 17:44, pass 1 English, first WSL run of the suite: 19 of 32 scenarios played (13 passed, 5 failed by defects of the suite, fixed later; 1 skipped), then the game ended with no final report (`exitReason: in-progress`). Not a verdict. Evidence deleted (superseded build and suite).
- 2026-09-22, English, shared run, partial for this mod: `01-loading` 5/5, `02-settings-page` 3/3 (the fixed settings page drawn with its scroll bar), `09`-`12` 0/5 because RIMMSQOL was not staged. Details in `2026-09-22-English-shared-run.md`. Evidence deleted.
- 2026-09-24 01:34, pass 1 English, full run of the 17 features: 46 scenarios reported passed, 0 failed, then silence from 01:52 (log stamps are UTC, 23:52 in the file) to the Pickle watchdog at 07:23. Cause: the laptop hit the critical battery level at 01:50 and went to sleep (Kernel-Power 524 then 42), not a defect of the mod or the suite. `exitReason` never written, so no verdict; the run counts for nothing towards `tested`. Rerun taken with the machine kept awake. Evidence not kept (the archive holds a Player.log only).
- 2026-09-24 08:38, pass 1 English, second attempt: the game crashed at startup, two minutes after the lock was taken (signal 11 in the Mono garbage collector, `GC_mark_from`, while the static constructors of the mods were running; exit code 139, no report, before any scenario). Not a verdict, and not a defect of the mod or the suite. Log kept in `Tests/Pickle/Evidence/2026-09-24-startup-crash/Player.log`. Third attempt queued (without `-EvidenceDir`, to keep its place in the queue).
- 2026-09-24 11:37, pass 1 English, third attempt: the game started and played 43 of the 60 scenarios in 45 minutes (35 passed, 1 failed, 7 skipped for lack of RIMMSQOL, all of `13-companion-rules` up to the bond, which passed for the first time), then Pickle's own deadline ended it (`exitReason: watchdog-timeout`, `RunTimeoutMinutes` 45 by default): the pass simply does not fit in 45 minutes. The one failure is `06-assists` "the mark over the companion": `I stop filming` timed out after 5 s because the film, taken every tick, ran at about one picture per second (10 pictures in 8 s) - the mark itself was found and the film was encoded. A defect of the film step under a heavy frame, not of the mod. No verdict. Files kept in `Tests/Pickle/Evidence/2026-09-24-pass1-deadline/`. Next attempt: film every 6 ticks in that scenario, `-RunTimeoutMinutes 80`, `-EvidenceDir`.
- 2026-09-24 16:38, pass 1 English: the fourth ticket (the whole pass in one, 80 min deadline) vanished from the queue without ever holding the lock (the queue was emptied between 14:22 and 16:20), and the pass is replanned as three small tickets, by `@part1`/`@part2`/`@part3` tags on the features (01-05, 06-08, 09-17), one process submitting them in turn. Results below as each part completes.
- 2026-09-24 16:40-17:10, pass 1 English, as three tickets on `6dff681` + uncommitted suite changes, mod DLL `99AEF88C…`, steps assembly built before 20:48: part 1 (01-05) 17/17 passed, part 2 (06-08) 13/13 passed (including the film scenario, now every 6 ticks), part 3 (09-17) 30 scenarios, 16 passed, 4 failed, 10 skipped (7 RIMMSQOL for pass 3, 3 without-Odyssey for pass 4, as intended). All three `exitReason` are `passed`/`failed` from a finished run, none is a watchdog. The 4 failures are defects of the suite, not of the mod: the cow scenario milked nothing because the colonist's Animals skill gives a 40 per cent chance of wasting the product (`CompHasGatherableBodyResource.Gathered`, `AnimalGatherYield`), and the three fishing scenarios found a lake with no fish because whether a lake has any is a single random draw (`WaterBody.SetFishTypes`). Fixed the same evening (skill 10 before every milking and shearing, the draw repeated up to 100 times); the steps assembly changed, so a final pass is due on the new build. Files in `Tests/Pickle/Evidence/2026-09-24-pass1-part1..3/`.
- 2026-09-25 02:54 and 02:59, two one-scenario checks of the fixes (cow milking `1fec`, lake fish `2908`): both failed at step 3 of the Background, `a colonist "Miner" exists`, with `Accessing map pawns off main thread` (`MapPawns.AssertMainThread`: the step ran off the main thread just after the save load, on a slow start: 36 s for the load against about 15 s in the three-part run). Neither scenario reached the code that was fixed, so the fixes are still unverified. No mod code is involved. One ticket with the four scenarios queued again (`f952`).
- 2026-09-25 03:2x, second try of the four failed scenarios (`f952`): the three fishing scenarios passed (the lake fix holds, and the fishing hook, reached for the first time, shows no thread error); the cow still gained 0 Milk, so raising the Animals skill did not remove the waste (a skill the colonist cannot use counts as level 0). The gathering step now repeats a wasted milking or shearing (animal refilled, up to 30 times) and names the yield stat if none lands. One ticket for the cow queued.
